[websocket]
server_address=172.17.0.1
server_port=18000
server_path=/deepstream/ws
protocol_name=deepstream-protocol

enable_ssl=false
auto_reconnect=true

# Maximum reconnection attempts
max_reconnect_attempts=-1
reconnect_interval_sec=5
connection_timeout_sec=10

# Message handling
max_message_size_kb=64
send_buffer_size=4096
receive_buffer_size=4096
heartbeat_interval_sec=30

# Performance settings
max_pending_messages=100
compress_messages=false
worker_thread_count=0

# Debugging and logging
enable_debug_output=false
log_message_content=false


# Health check settings
# Health check interval in seconds (0 = disabled)
health_check_interval_sec=30

# Health check timeout in seconds
health_check_timeout_sec=5

# Enable health check monitoring (true/false)
health_check_enabled=true


# Graceful shutdown settings
# Graceful shutdown timeout in seconds
graceful_shutdown_timeout_sec=10

# Pipeline stop timeout in seconds
pipeline_stop_timeout_sec=5


# Advanced reconnection settings
# Enable exponential backoff for reconnection (true/false)
exponential_backoff_enabled=true

# Maximum reconnection delay in seconds
max_reconnect_delay_sec=60

# Retry delay for server shutdown situations in seconds
server_shutdown_retry_delay_sec=10
